// let sum = (a,b) => {
//     console.log(a+b);
// }

const cube = (n) => {  //in single arg case we can remove the parenthesis
    console.log(n * n * n);
}

const pow = (a, b) => {
    return Math.pow(a, b);
}

//Implicit return(automatically return)
const mul = (a, b) => a * b;

const sum = (a, b) => a + b;
 